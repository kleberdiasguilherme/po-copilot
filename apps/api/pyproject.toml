[tool.ruff]
target-version = "py313"
line-length = 100
exclude = [".venv"]

[tool.ruff.lint]
# E/F = pycodestyle + pyflakes (padrao), I = ordenacao de imports,
# UP = sintaxe moderna, B = bugbear.
select = ["E", "F", "I", "UP", "B"]
